package com.berlin.telran.tasksJava.if_else;

public class MainIfElse {
    public static void main(String[] args) {

        // Вася спит днем если он не работает или если у него отпуск.
        //Дано:
        // Переменная weekday = true если это рабочий день
        // Переменная vacation=true если у Васи отпуск
        // Написать метод, возвращающий true  если Вася спит
        // public boolean isSleeping (boolean weekday, boolean vacation)
        // isSleeping(false,false)-> true
        // isSleeping(true,false)-> false
        // isSleeping(false,true)-> true


       // boolean weekday;
        // boolean vacation;
        // public boolean isSleeping (boolean weekday, boolean vacation);
       // if (isSleeping(false,false)) {
        //} else if



            // Воспитательница в детском саду знает, что если Петя и Вася оба смеются или
            // оба выглядят очень серьезно, то они что-то  затевают и надо быть внимательной.
            // Помогите воспитательнице и напишите метод, возвращающий true когда оба мальчика
            // смеются или ходят мрачные.
            // параметры petyaSmile and vasyaSmile показывают смеются ли они.
            // public boolean kidsTrouble (boolean petyaSmile, boolean vasyaSmile)
            // kidsTrouble(true,true)->true
            // kidsTrouble(false,false)->true
            // kidsTrouble(true,false)->false




    }
}
